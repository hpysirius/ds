# 2026-09-23

## 插件"连不上后端"排查（Chrome 142+ LNA）
- 现象：插件报"连不上后端"，但后端 3101 活着，curl 带 chrome-extension Origin + PNA 预检全通过。
- 结论：Chrome 142（2025 末）起强制 **Local Network Access (LNA)** 权限，发往 localhost 的请求被拦时
  fetch 静默失败（无细节）。扩展理论上凭 host_permissions 豁免，但 2026 版本行为收紧。
- 插件侧修复：`safeFetch` 统一加 `targetAddressSpace: 'local'`（官方推荐声明，需要授权时弹提示而非静默失败）；
  `normalizeApi` 规范化地址（漏写 http:// 会直接 fetch 失败）；popup 不再自己 fetch，统一走 background 的 DS_PING。
- 用户侧解法（按顺序）：① 核对弹窗地址 http://localhost:3101 ② 在普通标签页打开
  http://localhost:3101/pricing/extension/pending?limit=1 应显示 JSON ③ chrome://settings/content/localNetworkAccess
  允许 ④ chrome://flags/#local-network-access-check → Disabled → 重启 Chrome（M152 前有效）。

## 09:25 LNA 终极绕过：localhost 中继（v1.1.0）
- 昨晚的 targetAddressSpace 声明不够：扩展的 service worker **无法触发 LNA 授权弹窗**（Chrome 限制），被拦就是静默失败。
- 方案：利用 Chrome 对「本地→本地」请求的 LNA 豁免 —— safeFetch 直连失败时，自动在
  **localhost:3100（ds 前端）的标签页**里用 chrome.scripting.executeScript 执行 fetch 到 3101。
  - 3100 在后端 CORS 白名单（FRONTEND_URL 默认值），跨端口 CORS 已 curl 验证通过
  - 中继标签 active:false 放后台，按 url pattern 查找复用
  - manifest host_permissions 增加 3100（v1.0.0→1.1.0），**改了 manifest 必须刷新插件**
- 以后凡是「扩展连不上本机服务」的问题，先想到 LNA，用 localhost 页面做中继是最稳解法。

## 09:30 真相：不是 LNA，是地址填错
- 用户把插件地址填成了 http://localhost:3100（前端）→ /pricing/extension/* 404。
- **能收到 404 说明扩展→localhost 的 fetch 是通的，Chrome 并没有拦**（昨晚的 Failed to fetch 大概率发生在我重启服务的空窗期）。
- 复现验证：3101 返回 200，3100 同路径返回 404。
- DS_PING 加了防呆：404 时明确提示"3100 是前端页面，请改填后端 3101"。
- 教训：排查"连不上"要先区分「网络层失败(Failed to fetch)」vs「收到 HTTP 错误(404/500)」——后者网络是通的。

## 09:30 popup 输入框被轮询覆盖的 bug
- 症状：用户在地址框输入 3101，会"自动变回"3100，保存也无效。
- 根因：refresh() 每 2s 执行 `$('api').value = r.api`，无条件覆盖输入框 → 用户输入被打断冲掉，
  点保存时框里已是旧值，等于把 3100 又存了一遍。
- 修复：`document.activeElement !== $('api')` 时才更新输入框；保存后 blur + 按钮反馈"已保存 ✓"。
- 教训：任何定时刷新 UI 的代码，都不要碰正在被用户编辑的控件。

## 09:40 插件对接远程服务器（v1.2.0）
- 服务器 http://114.132.99.141 （admin/admin123），nginx 反代：前端 `/`，接口 `/api`。
  **插件地址应填 `http://114.132.99.141/api`**（不是裸 IP，也不是 /api 之外的路径）。
- 已在服务器验证：/api/pricing/extension/pending 200、POST /api/pricing/extension/products 201、
  CORS 放行 chrome-extension Origin。**公网 IP 不受 Chrome LNA 限制，无需本地中继。**
- 服务器商品库当前为空（products total=0，pending remaining=0）→ 插件连上后「待补商品」显示 0 属正常，
  需先用「采集当前列表页」往服务器灌数据。
- 插件改动：
  - manifest v1.2.0，host_permissions 增 `http://114.132.99.141/*`（改 manifest 必须刷新插件）
  - `isLocalApi()`：只有 localhost/127.0.0.1 才走 LNA 中继，远程地址直连失败直接报错（不再瞎开中继页）
  - 中继页地址改用 `new URL(api)` + port=3100 推导，避免字符串替换在非常规地址下算错
  - 404 防呆分三种：本地→提示改 3101；远程缺 /api→提示加 /api；远程已带 /api→提示服务器代码未更新
  - popup 增加「填本地 / 填服务器」一键按钮（PRESET_LOCAL=3101，PRESET_SERVER=http://114.132.99.141/api）

## 09:45 填服务器地址却 Failed to fetch —— targetAddressSpace 用错（v1.2.1）
- 症状：地址填 http://114.132.99.141/api，curl 全通，但插件报"连不上后端"（网络层失败，非 HTTP 错误）。
- 根因：**给公网地址也加了 `targetAddressSpace: 'local'`**。Chrome 的实际地址空间（public）与
  声明（local）不符 → 直接判网络错误 Failed to fetch。本地 3100/3101 因为确实是 local 才没暴露。
- 修复：`isLocalApi(url)` 为真才加该声明；远程地址直连，失败时给出针对性提示
  （服务器在线 / 末尾带 /api / 防火墙）。
- 附带两个同类坑：
  1. DS_PING 的 catch 用通用 NET_ERR 覆盖了 safeFetch 的详细信息 → 改为 `(e && e.message) || NET_ERR`
  2. popup 拼接成"连不上后端（连不上后端：…）"套娃 → 改为原样展示 p.error
- 经验：**targetAddressSpace / PNA 声明只对真·本地地址用**，对公网服务会适得其反。

## 23:50 第三方选品插件污染标题 + 经营指标入库（v1.3.0）
- 现象：商品库标题变成「中实跨境ERP选品标签：类目：---rFBS佣金：…」——第三方选品插件（s2-widget 前缀）
  把浮层注进 Ozon 卡片 DOM，collectList 找"第一条够长文本"当标题时抓到了浮层。
- 插件修复（collector-lib.js）：
  - 列表采集跳过「在 s2-widget 里 / 子树含 s2-widget」的元素（WIDGET_SEL + touchedByWidget）
  - 标题门槛 8→6 字，剔除"仅剩 N 件/остал"库存文案
  - 列表页每张卡抓 `[data-s2-field-key]` → item.pluginCard 上报；rfbsCommission 三档用 "/" 连接防假数字
  - 详情页 host 增加 .s2-widget-ready-content + 全页兜底扫描
- 后端（sourcing.service.ts）：
  - 新增 WIDGET_TITLE_RE（中卖搏通ERP/中实跨境ERP/选品标签：）→ 三条上报路径都拒收这种标题
  - 插件卡映射抽成模块级 applyPluginCard()，persistProbe 与 ingestProductList 共用
  - 新映射：volume→sizeLengthMm/Width/Height，weight→sizeWeightG（支持 kg/g/кг/г），category 末段→category3Name
  - 其余无列字段（rfbsCommission/promoRevenueShare/offers/offerMinPrice/historicalAvgPrice 等）存 raw.pluginCard
- 数据修复：本地库 108 条污染标题已清空（备份 /tmp/ds-polluted-titles-backup.json），补详情会自动重抓。
  服务器如有同类脏数据执行：UPDATE products SET title=NULL WHERE title LIKE '%选品标签：%';
- 本地后端已重启生效；服务器要 git pull + 重启后端才有新映射逻辑。
- 教训：页面上有任何第三方注入 DOM 时，采集选择器必须显式排除注入物（closest + querySelector 双查）。
