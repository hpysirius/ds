# 2026-09-19

## 把《定价表模版最新.xlsx》的核价功能做进系统

来源文件：`/Users/huanghui/Downloads/定价表模版最新.xlsx`（13 个 sheet，核心是「定价表」
「GUOO realFBS资费试算表」「白俄专线」「哈萨克斯坦专线」「俄罗斯公式」「吉尔吉斯公式」）。
用 Python 标准库解 zip + 解析 sharedStrings/sheet XML 读出的（无网络装不了 openpyxl）。

### 落地的东西
- Prisma 新增三张表：`logistics_channels`、`pricing_records`、`pricing_settings`
  （用 `npx prisma db push` 同步，MySQL 容器 `playlish-mysql` 在跑）
- 后端新模块 `backend/src/modules/pricing/`
  - `pricing.calc.ts` 纯函数引擎：计费重量 / 运费 / 毛利润 / 净利润 / 利润率 /
    运费利润比 / 加35% / 反算定价
  - `channels.data.ts` 111 条内置渠道种子（GUOO 俄罗斯15 + 白俄12 + 哈萨克12；
    兴远 XY 俄罗斯36 + 吉尔吉斯36）
  - 接口：`/pricing/meta|settings|channels|quote|calc|records|from-product/:sku`
- 前端新页 `frontend/src/app/pricing/page.tsx`（Tabs：核价计算 / 物流渠道 /
  核价记录 / 参数设置），侧边栏加了「核价计算」入口
- README 补了核价章节

### 关键公式（照搬 Excel，不要自己发明）
```
毛利润 = 定价 − 采购成本 − 国际运费 − 贴单费 − 定价×平台佣金 − 定价×Ozon代理佣金
净利润 = 毛利润 − (采购成本 + 毛利润) × 提现费率
利润率 = 净利润 / 采购成本；运费利润比 = 净利润 / 国际运费；加35% = 定价 / 0.65
反算定价 C = [目标净利 + D·i + (D+E+F)(1−i)] / [(1−g−h)(1−i)]
运费 = 计费重量 × 元/kg + 元/票；XY 走 ROUNDUP(x,2)，GUOO 不取整（保留4位）
```
已用模版样例校验：定价37/运费16 → 毛利润5.265、净利润5.10582、利润率0.6382275；
GUOO 俄罗斯 0.36kg/18×18×10/1000₽ → 13.486 / 17.518 / 21.568，与表内一致。

### 踩到的坑
- `nest start --watch` 会清 `dist`，被 safe-delete 守卫拦下（148 文件 > 50 阈值）。
  改用 `npm run build && npm run start:prod` 起服务。
- 全局 ValidationPipe 开了 `forbidNonWhitelisted`，DTO 必须列全字段；
  `Partial<XxxDto>` 是纯类型、运行时 metatype 变 Object 会跳过校验，
  所以 PATCH 要单独写一个全字段可选的 `UpdateChannelDto`。
- `recompute()` 直接 spread `calcPricing()` 结果会把 grossMargin/netMargin/costTotal
  这些非落库字段塞进 Prisma create 而报错，要显式挑字段。
- Prisma Decimal 返回的是对象，服务层统一用 `num()` 转 number。

### 原表数据疑点（已在渠道备注里标注，可改）
俄罗斯公式 H18（XY Express Small）运价系数填 16、H30（XY Express Premium Small）填 26，
与「元/克」描述（0.045 = 45元/kg）对不上，疑似笔误；种子数据按表内数值原样保留，
需要时到「物流渠道」页改。
